// set inital sum variable (maybe outside the loop)

// create a for loop that logs nums 0 -10 (initially)
  // lets now check for the numbers that are even, ignore numbers that are odd
  // add the even numbers to our sum vairable

// outside of the for loop lets log the result of our sum.

// let evenSum = 0
//
// for (let i = 1; i <= 10; i++) {
//   if (i % 2 === 0) {
//     evenSum += i
//     // console.log(evenSum)
//   }
// }
//
// console.log(evenSum)
